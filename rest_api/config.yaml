# Restricted YAML-like config format.
# Supported shape:
# section:
#   key: value

server:
  host: mysqlserver
  port: 3004
  threads: 4
  allowed_origins: http://192.168.1.20:8084

mysql:
  host: test_rest_DB
  port: 18080
  user: dev_admin
  password: MYSQL_PASSWORD
  database: rest_api
  pool_size: 8
