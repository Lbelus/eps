# Restricted YAML-like config format.
# Supported shape:
# section:
#   key: value

server:
  host: mysqlserver
  bind_addr: 127.0.0.1
  port: 3004
  threads: 4
  allowed_origins: https://eps.example.com

mysql:
  host: test_rest_DB
  port: 18080
  user: dev_admin
  password: MYSQL_PASSWORD
  database: rest_api
  pool_size: 8
